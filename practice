#markdown使用方法
前后两个*或者下划线或CTRL加b为加粗，一个或许ctrl加i为斜体，引用用大于号，#几级标题
>引用
无序列表
- + *，有序列表前有数字，可以嵌套，todulist 
-[ ] a记得空格，可以编辑打勾打叉
换行要两个空格再换行，或者中间空一行
分割线三个以上的*
高亮== == 三个*** ***斜粗体，删除前后两个波浪号，下划线<u> </u>


#todolist
1.github注册，git安装，学会版本控制，分支控制，markdown基本语法，安装pycharm，anaconda，jupyter  
2.学习python，了解三大库语法，学习json库  
3.学习线性代数的矩阵运算，矩阵与向量关系，线性变换，点乘叉乘，矩阵求导；图论与多智能体系统一致性，回答ppt思考题；学习微分基础，多元微分  
4.个人GitHub仓库链接发给导师，markdown笔记上传GitHub，熟悉运用python  
5.作业python代码
 
#数学笔记  
##多元微分  
1.偏导数  
设z=f(x,y)  
对x偏导，fx(x,y)=∂z/∂x  
对y偏导，fy(x,y)=∂z/∂y    

2.全微分  
dz=（∂z/∂x）dx+（∂z/∂y）dy  
可微推出连续+偏导存在  
偏导连续推出可微  
3.复合函数求导（链式法则）  
若z=f(u,v)  u=u(x,y)  v=v(x,y)  
则∂z/∂x=(∂z/∂u)(∂u/∂x)+(∂z/∂v)(∂v/∂x)  
求z对y的偏导同理   

4.隐函数求导  
方程F(x,y,x)=0,确定z=z(x,y)  
有∂z/∂x=-Fx/Fz  
∂z/∂x=-Fx/Fz   

5.多元函数极值  
步骤：  
·求一阶偏导，令为0，找驻点  
·求二阶偏导  
A=fxx，B=fxy，C=fyy  
·算判别式  
∆=B^2-AC  
若∆<0,A>0,则有极小值，将x，y值代入原函数即可得  
若∆<0,A<0,则有极大值  
若∆>0,则(x0,y0)不是极值点  
若∆=0，该方法无法判断，要用其他方法(定义法，泰勒展开)   
eg：f(x,y)=x^3+y^3-3xy  

6.方向导数&梯度  
·方向导数  
∂f/∂l=fxcosa+fycosb  
·梯度gradf=(fx,fy)  
沿梯度方向，方向导数最大    
eg:f(x,y)=x^2y,求在(1,2)沿l=(1,1)的方向导数  
   
     
##线性代数  
###矩阵运算  
  
###矩阵与向量的关系  
  
###线性变换  
  
###点乘  
  
###叉乘  
  
###矩阵求导  
  
    
##图论与多智能体系统的一致性


#GIT分支  
1.主分支=项目主线，功能分支=独立开发，平行宇宙=互不干扰  
2.先各自负责，最后合并，但最好在中间过程中就进行几次合并，否则总和并会很难受  
3.常见分支类型  
·main/master主分支  
·develop整合日常的开发工作  
·feature开发具体的新功能  
·hotfix用于紧急修复bug  
4.如何查看GIT仓库中所有分支  
在终端使用git branch命令来列出分支列表，命令的输出展示了仓库中所有的本地分支，其中带有*的main分支表示我们当前所在的位置，颜色区分不同类型的分支  
5.如何创建一个新的git分支  
使用git branch命令加上空格加上分支名即可创建新分支，但这个命令并不会自动切换到新分支，此时我们仍在main分支上  
6.如何切换分支  
·使用传统的git checkout命令加空格加分支名，此时，工作区的文件会自动更新为目标分支的内容  
·git switch加空格加分支名更加直观，效果相同，但语意更加清晰，更专注于切换操作，工作区也会同步更新为目标分支的状态  
7.创建的同时切换分支  
使用git checkout -b空格加分支名，b为branch缩写，一个命令完成两个任务  
8.如何在分支上工作  
·修改代码，将更改加入暂存区，接着提交更改，每次提交都会在当前分支上创建一个新的提交点，完成本地提交后，可将更改推送到远程仓库  
·分支上一切操作都是相互独立的，不影响主分支  
9.如何合并分支，把特性分支的改动集中到主分支上  
step1：先切换到目标分支，也就是主分支main，输入git checkout main
step2：使用merge命令把特性分支合并进来，即输入git merge空格加分支名  
执行命令后，git会生成一个特殊的合并提交，将两个分支的历史连接到一起，并把特性分支中的所有更改完整地集成到主分支中合并完成  
10.删除分支  
当成功合并到主分支后，通常会清理已完成的分支，使用git branch -d空格加分支名的命令来删除它，只有分支的改动已经完全合并才允许被删除，删除后，git会移除这个分支的引用，但所有的提交历史依然会保留到git的记录中，随时可以追溯查看  
11.解决合并冲突  
当合并遇到冲突时，按标准流程解决，冲突通常发生在同一个文件的相同位置，被不同分支做了修改，git无法自动判断该保留哪一方的更改，解决冲突一般分四步：  
·查看冲突的文件  
·手动编辑内容，解决差异  
·将已解决的文件添加到暂存区  
·执行一次合并提交  
12.分支管理最佳实践  
·命名规范，使用清晰的前缀标识分支类型  
eg：feature表示新功能开发，bugfix用于修复一致问题，hotfix专用与紧急修复  
·定期同步，经常拉取主分支，及时合并变更，避免分支过期  
·保持整洁，合并完成的分支及时删除，特性开发避免拖延，小步快跑频繁短周期的迭代  
#第一次推送分支：git push -u origin 分支名     

#vs code如何在修改后上传到github  
打开终端，输入git add .回车，将所有文件添加到暂存区，输入git commit -m "提交说明"回车提交到本地仓库，再输入git push回车推送到GITHUB    
或者在更改中提交
  
 
#python基础知识
1.python文件名最好不要有空格和特殊符号，英文数字下划线最好  
2.退出代码为0，表示运行没有错误  
3.字符串连接：把每个字符串用引号引起来之后，之间用加号连接，可打印在一起，若引号内字符前没有空格，就会直接连在一起。加号和引号之间加上空格更加美观。但是不能在每一行的开头随便加空格，否则会报错。
4.单双引号转义：绝大多数情况下，单双引号是一样的，但若内容本身就有引号，要留意是单还是双，避免内外一样导致配对出现问题，或者在字符串的引号前放上\  来表明后面的是单纯的引号，是内容的一部分，不表示结束。\叫转义符  
5.换行：python中一句代码不能随便换行，因为python读一行，执行一行，读到第一行结尾，看你没有结束引号和括号，以为你忘了，就会直接报错，根本不会继续往下读第二行了，可用\n来换行  
6.三引号跨行字符串：三个连在一起的单引号或双引号，用它包裹住文字，python就会把新的一行当作内容的换行，而不是代码语句的结束，对于打印跨行多的内容非常方便。  
7.变量用于储存或者指代值的，先给变量取名，变量名中间不能有空格，也不能是数字开头，也不能用引号包裹，有引号就会变成字符串。名称加等号加变量对应的值，eg：my_love = "123456789",这个叫赋值操作，赋值后，可通过变量名直接获取值，不需要每次再打一遍数值。先对变量赋值，然后才能用变量，否则会报错。  
8.也可以用变量给变量赋值，eg；my_love = "123456"  
                            my_ex = my_love  
                            my_love = "150789"  
等号左边被视为变量名，等号右边会先被求值，把原先的值分给左边，最好，右边被赋予新的值，这样可以同时保存之前的和现在的。  
9.变量的好名字：  
·少用拼音，用英文单词，python约定俗成下划线命名法，字母全部小写，不同单词用下划线隔开，user_age,或驼峰命名法，用大写字母区分不同单词，UserAge  
·python是大小写敏感的，user_age 不等于 user_Age，会被看作两个不同的变量  
·变量名不要占用python关键字，如print，打出来的变量名是彩色的，说明是关键字，最好换成其他的  
10.python中整数不用引号包裹，运算级和数学一样，先乘方，再乘除，最后加减乘方用两个*，2的三次方2 ** 3，若要进行开方，取对数之类的负责运算，需要导入math函数库，导入方法为在文件最开始一行写import math，然后用math.函数名（...）使用功能  
eg计算sin1，写import math换行math.sin(1)  math.log2(8),但只是计算之后放在了内存里，没有被后续使用，也没有被打印出来。想知道结果可以加上print以及（）  
11.代码里，4a乘号不能省略，应该是4 * a    
12.python中在一行的开头加上#即表示注释，或者不想要读取这一行代码，也可以加注释，但是只能管一行  
若要注释一大段，选中那一段，按CTRL和斜杠/，这样一整段代码前都会自动加上＃若想要撤销，选中再按一次ctrl和/即可  
或者用前后"""包裹住注释，只要不打印不进行其他操作就可以  
13.数据类型  
·字符串str：被单或者双引号包裹，对字符串使用len函数，可以得到该字符串长度len('hello')，空格数字符号都会占据一个长度，\n表示换行，站一个长度。可以在字符串引号外边放上[某个数字]，表示索引，程序从0开始数。  
 ·整数int  浮点数float   
 ·布尔类型bool ：只包含两种值，即真和假，True和False，首字母要大写  
 ·空值类型None Type：只有一种值None，表示完全没有值，如果需要一个变量，但还不知道这个变量的值，就可以先定义为None  
 ·当不确定某个对象的类型时，可以用type函数，会返回该对象类型，eg type(6.0)  
 ·len用于得到字符串长度，不能作用于整数，浮点数，会报错  
 14.python的两种执行模式
 命令行模式 ：写好命令后，保存并运行整个文件，运行时，python解释器会一行一行对文件进行解析和执行
 交互模式：不需要创建新文件，只需要进入交互式环境，点击pycharm下方的python控制台，或者在windows的CMD Mac的终端里输入python3，可以看到三个尖括号>>>,表示在等待我们的指令。输入后，不需要print语句，就可以直接看到执行输出的结果。若要推出交互模式，输入quit()回车，或者直接按ctrl+d。交互模式中的命令都不会被保存，我们平时多用命令行模式。
 15.python的input函数可以从用户那里获取输入，input("这里是给用户的一些提示"),一般用一个变量获取input函数返回的值，eg；print("知道了，你今年"+ 变量名 + "岁").  
 input一律返回字符串，可以用int()来返回整数，float()转换成浮点数，str转换成字符串。整数不能和字符串连接在一起打印，可以把整数转换成字符串，2变为str(2)
 16.#BMI = 体重 / (身高 ** 2)
user_weight = float(input("请输入您的体重（单位：kg）:"))
useer_height = float(input("请输入您的身高（单位：m）:"))
user_BMI = user_weight/ (useer_height) ** 2
print("您的BMI为："+str(user_BMI))  
17.条件语句  
通过判断条件是否满足，来执行，建议执行语句前缩进为四个空格，语句也可以为多个结构为：  
if [条件]：  
    [执行语句]   
    [执行语句]      
else：  
    [执行语句]
求值出来的布尔值都可以作为条件，若定义某个变量值为布尔值，那它就可以作为条件  
eg：is_happy = True  
if is_happy:  
也可以结合比较运算符，3==3，会返回True，不等于号！=，大于小于号  
